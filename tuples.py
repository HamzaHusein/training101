# tuple is similar to a list

things = ("trees", "fields", "tables", "kitchen", "chairs", "socket")

print(things[1:])
print(things[0:3])

# no one can manipulate a tuple while list can be manipulated meaning changed.
